This folder contains a self-contained implementation of System C (see wiki) built from scratch.
I decided to start from scratch with System C because, as a result of so much experimentation, the code I have is super messy.
I expect that this implementation will end up mostly being an e-graph library built just for System C.  

